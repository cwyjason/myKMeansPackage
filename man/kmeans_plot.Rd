% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kmeans_functions.R
\name{kmeans_plot}
\alias{kmeans_plot}
\title{Plot K-means clustering results}
\usage{
kmeans_plot(
  data,
  kmeans_result,
  x_col = 1,
  y_col = 2,
  main = "K-means Clustering Result",
  point_size = 1.5,
  center_size = 3,
  color_palette = NULL,
  ...
)
}
\arguments{
\item{data}{A numeric matrix or data frame with 2 columns for plotting.}

\item{kmeans_result}{The result from kmeans_cluster function.}

\item{x_col}{Index or name of the column to use for x-axis. Defaults to 1.}

\item{y_col}{Index or name of the column to use for y-axis. Defaults to 2.}

\item{main}{Title for the plot. Defaults to "K-means Clustering Result".}

\item{point_size}{Size for data points. Defaults to 1.5.}

\item{center_size}{Size for cluster centers. Defaults to 3.}

\item{color_palette}{A vector of colors for the clusters. If NULL, a default palette is used.}

\item{...}{Additional parameters passed to the plot function.}
}
\value{
A plot of the clustered data (invisibly returns NULL).
}
\description{
This function creates a scatter plot of the clustered data and highlights the cluster centers.
}
\examples{
# Generate sample data
set.seed(123)
data <- matrix(rnorm(100), ncol = 2)

# Perform clustering
result <- kmeans_cluster(data, centers = 3)

# Plot the results
kmeans_plot(data, result)

# Customize the plot
kmeans_plot(data, result, main = "My Custom Clustering Plot",
            point_size = 2, center_size = 4)

}
